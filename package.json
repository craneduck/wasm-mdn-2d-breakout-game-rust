{
  "name": "wasm-mdn-2d-breakout-game-rust",
  "version": "0.2.0",
  "author": "craneduck",
  "license": "MIT",
  "description": "``` $ wasm-pack build --target web ```",
  "main": "index.js",
  "scripts": {
    "wasm": "wasm-pack build --debug ./ --target web",
    "wasm-release": "wasm-pack build ./ --target web",
    "dev": "npm run wasm && vite",
    "build": "npm run wasm-release && vite build",
    "lint": "biome lint ./web/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.9.4",
    "typescript": "^5.8.3",
    "vite": "^6.3.4"
  }
}
